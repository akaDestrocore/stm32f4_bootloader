[workspace]
members = [
    "boot",
    "loader",
    "updater",
    "application",
    "shared",
    "drivers",
]

# Shared optimization settings
[profile.release]
codegen-units = 1
debug = true
lto = true
opt-level = "z"

[profile.dev]
opt-level = "z"